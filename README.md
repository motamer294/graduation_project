# 🎫 Help Desk - Customer Support Ticketing System

هذا المشروع هو نظام متكامل لإدارة تذاكر الدعم الفني، يهدف إلى تنظيم طلبات العملاء وتوزيعها على الموظفين ومتابعة حالة الحل بشكل احترافي.

---

## 🛠️ تقنيات المشروع (Tech Stack)
* **Backend**: Django & Django Rest Framework (DRF).
* **Database**: PostgreSQL (Running on Docker).
* **Environment**: Docker & WSL2.

---

## 🚀 تعليمات الإعداد لفريق العمل (Setup Guide)

لضمان عمل المشروع بنفس الكفاءة لدى الجميع، يرجى اتباع الخطوات التالية بدقة:

### 1. المتطلبات (Prerequisites)
- تثبيت **Docker Desktop**.
- تثبيت **Python 3.12**.
- استخدام **WSL2 (Ubuntu)** إذا كنت تعمل على Windows.

### 2. تشغيل المشروع لأول مرة
افتح الـ Terminal داخل مجلد المشروع ونفذ:

```bash
# 1. إنشاء وتفعيل البيئة الافتراضية
python3 -m venv .venv
source .venv/bin/activate

# 2. تثبيت المكتبات اللازمة
pip install -r requirements.txt

# 3. تشغيل قاعدة البيانات عبر دوكر
docker-compose up -d

# 4. إنشاء الجداول في قاعدة البيانات
python manage.py migrate
